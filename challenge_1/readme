**Terraform commands to execute**

# Terraform Initialize
```terraform init```

# Terraform Validate
terraform validate

# Terraform Plan
terraform plan

# Terraform Apply
terraform apply -auto-approve
